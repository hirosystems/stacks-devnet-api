apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stacks-api-pvc
  namespace: "{namespace}"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    limits:
      storage: 750Mi
    requests:
      storage: 500Mi
  storageClassName: devnet-storage-class
  volumeMode: Filesystem
